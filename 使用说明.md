# AI 工作流创作首页 · 案例编辑版

## 打开本地预览

预览地址：http://127.0.0.1:4173

当前预览已启动。以后重新启动时，在 PowerShell 中运行项目里的 `启动本地预览.ps1`，保持窗口打开，再访问上面的地址。若提示端口已占用，先检查原预览是否仍在运行。

请始终使用同一个浏览器和这个地址。换浏览器、换端口或直接双击 HTML，属于不同的浏览器存储位置，不会自动显示原来保存的案例；可以用案例包迁移。

## 编辑案例

1. 开启“编辑案例”，在对应工作流卡片中分别上传“原始素材”和“生成效果”。
2. 图片分类两侧接收图片；视频分类的原始素材接收图片或视频，生成效果接收视频。单份素材最多 200 MB，视频优先使用可播放的 H.264 MP4 或 WebM。
3. 上传成功后，状态栏显示“已保存到此浏览器”。可分别替换或删除两侧素材。
4. 两侧都有素材时，拖动中间分割线对比；也可以聚焦滑块后使用方向键。只有一份素材时正常展示单份。
5. 视频通过卡片下方的播放按钮和进度条统一控制，默认静音。两段视频时长不同时，以较短视频为对比播放终点；分别查看素材可播放完整视频。
6. 关闭“编辑案例”检查展示效果。展示状态下，点击卡片图片、标题或空白区域会自动填入对应提示词模板；“使用 →”按钮不再显示。拖动分割线、播放视频、调整进度和查看原文件不会触发填入。也可以聚焦卡片后按 Enter 或空格使用模板。

## 尺寸规则

- 卡片宽度随页面列宽调整，高度优先按生成效果比例确定；没有生成效果时使用原始素材比例。
- 两侧素材各自等比完整显示，比例不同会留白，不会拉伸或裁掉图片。
- 极端长图/宽图的预览比例限制在 1:2 到 3:1 之间，点击“查看原始素材/生成效果”可查看完整文件。
- 工作流在当前列数下有固定列归属，更换素材只影响同列下方卡片。窗口大小跨越列数断点时统一重排。

## 保存和备份

案例素材保存在当前浏览器的本地数据库中，刷新后恢复。没有上传到远程服务，也不会写回 HTML。

- 只有开启“编辑案例”时才显示导入、导出按钮。
- “导出案例包”：下载 `.aicases` 文件，包含全部 16 个工作流的案例状态、素材字节和对应提示词模板正文；未配置模板明确保存为空。
- “导入案例包”：先校验素材，再显示确认窗口。确认后按包内工作流整体替换前后素材，空案例也会恢复为空；包外工作流保持不变。
- 新版案例包同时恢复素材和提示词模板，刷新后保留；旧版案例包仅恢复素材，保留当前模板。第 3 版案例包同时恢复工作流名称，旧版包保留现有名称；包外工作流保持不变。
- 素材与模板在同一保存事务中写入。损坏包不写入，取消导入或保存失败会保留原数据。
- 清理浏览器数据、迁移电脑或浏览器前，请先导出备份。

## 文件用途

| 文件 | 用途 |
| --- | --- |
| ai_workflow_prototype_v7_auto_panel.html | 页面入口，沿用原文件名 |
| styles.css / cases.css | 原首页样式及案例编辑、对比、响应式样式 |
| workflows.js | 16 个工作流标题与原有 5 份提示词定义，后续更新模板的位置 |
| cases.js | 案例上传、对比、排布、本机保存、案例包导入导出 |
| composer.js | 原顶部创作输入、素材上传、模式和参数选择 |
| preview-server.cjs / 启动本地预览.ps1 | 本地预览服务与启动入口 |
| 执行计划与验收.md | 分批执行计划、验收结果与实现边界 |
| tests/ | 自动验收脚本及检查记录，测试素材不会进入实际预览 |

本版仍是交互原型，顶部未接入真实 AI 生成服务。默认仍有 11 个工作流没有模板；可通过新版案例包导入对应模板。未配置或为空时，选择会提示并保留当前输入。本次未新增页面内模板编辑器。

## 修改工作流名称

开启“编辑案例”后，每张卡片显示名称输入框。修改后点击“保存名称”或按 Enter，名称会保存到当前浏览器，刷新后保留。名称限 1–40 个字符，不可全为空格。退出编辑后隐藏名称输入框。搜索使用修改后的名称，素材和提示词仍绑定原工作流。第 3 版案例包包含名称，可一起迁移。


## 项目默认案例

通过 `node preview-server.cjs` 启动并打开本地预览地址。首次访问、且浏览器没有已有案例数据时，自动载入 `public/defaults/default.aicases`。已有本地名称、素材和模板不会自动覆盖。

默认包来自用户提供的 2026-09-09 案例包，保留 16 个名称，按提示词文档更新 11 份模板；5 个待定工作流模板为空。源案例包不含图片或视频，因此卡片初始显示待添加案例。

文档“场景生成”对应卡片“场景风格改变”，“产品四视图白底图生成”对应“产品四视图白底图”。

用户可开启编辑，导入自己的案例包；点击“加载项目默认案例”并确认可恢复项目默认内容。导入只影响当前浏览器。若要让所有新用户使用新版案例，将导出的文件替换为 `public/defaults/default.aicases` 并提交 GitHub；已有用户可手动加载项目默认案例。不要直接双击 HTML，请通过本地预览服务打开。


## Vercel 静态部署目录

网站文件统一放在 public/，入口为 public/index.html；本地预览服务也读取此目录。仓库 vercel.json 指定 Other、无需构建或安装依赖、输出目录 public。Vercel 项目根目录保持 ./。提交 main 后可通过已连接的 GitHub 集成触发部署。
