# 本地原型改造计划

## 批次与验收目标
1. 基础整理：删除内嵌案例资源，保留 10 个图片工作流、6 个视频工作流和 5 份现有提示词；增加编辑开关。
2. 案例展示：独立双素材上传、替换、删除；单素材回退；图片/视频分割对比；视频共同播放/进度；比例自适应与固定列归属；查看原文件。
3. 保存迁移：浏览器本地自动保存；导出/导入二进制案例包；导入先验证再确认，事务写入；失败保留原数据。
4. 验收交付：验证素材生命周期、刷新恢复、包往返、无效文件、标题模板不变、布局、移动端、键盘及原顶部交互；提供本地预览。

## 原始模板校验
原始 workflows 定义（去除素材）SHA256：8a46772e89b5d5aa13534247cbed3882ace5ff4ec313473a36d5dbfff174cf5b

## 约定
- 以生成效果比例为优先；不拉伸、不裁剪；极端比例限制预览高度，可查看原文件。
- 固定列归属，同列下方卡片随尺寸变化移动。窗口列数变化时统一重排。
- 本地浏览器保存，使用固定预览地址；案例包用于迁移和备份。
- 原来没有模板的工作流不新增模板，后续统一更新。

## 执行结果（2026-09-07）

四批已完成。自动验收使用独立 Edge 无头浏览器会话，测试素材未写入实际预览。共 15 组检查全部通过，未捕获脚本错误为 0。

- [x] 初始清空素材；10 个图片卡片；工作流与提示词校验一致
- [x] 双素材独立上传；单素材回退；以效果比例适配；完整显示；其他列位置稳定
- [x] 键盘调整对比分割线；对比不误填模板；使用按钮填入原模板
- [x] 极端比例限制高度；可打开原始尺寸素材并用 Escape 关闭
- [x] 刷新恢复前后素材；展示模式隐藏编辑入口且保留对比
- [x] 替换、删除及删除后的刷新恢复正确
- [x] 损坏图片拒绝保存，原素材不受影响
- [x] 搜索及空结果反馈；视频分类保留 6 个工作流
- [x] 视频双素材对比、共同播放暂停、进度跳转、结束停止
- [x] 图片转视频混合素材对比可用
- [x] 案例包完整往返；取消不修改；导入恢复全部素材且不改提示词
- [x] 截断案例包拒绝导入，原保存内容保持完整
- [x] 原顶部素材上传删除、比例与质量选择仍可操作
- [x] 320/390/768/1024/1440 宽度布局；移动端无横向溢出；参数面板可见
- [x] 浏览器无未捕获脚本错误

## 视觉检查

已检查桌面空状态、不同图片比例的对比和瀑布流、视频对比，以及移动端布局。前后两层分别等比显示，不随分割线改变素材自身尺寸。

## 实现边界

- 本次仍为本地交互原型，未接入真实 AI 生成服务。
- 本机保存与浏览器及访问地址绑定；案例包提供迁移备份。
- 视频提供共同播放与进度校正；不同镜头/动作的视频不会自动做语义或逐帧匹配。
- 未对所有视频编码做兼容性保证，无法解码时会给出提示并保留旧素材。
- 原工作流中已有 5 份提示词，其他工作流未新增模板。

## 本地预览

http://127.0.0.1:4173

后续启动方式见《使用说明.md》。


## 本次修改与验收（2026-09-09）

以下记录更新前述旧版行为约定。

1. 已完成编辑状态切换：导入、导出按钮仅编辑开启时显示，退出编辑隐藏“使用 →”。
2. 已完成整卡点击：展示状态单击图片、标题、空白区域自动填模板，支持键盘；对比拖动和独立媒体控件不误填。
3. 已完成案例包 format 2：包含 16 个工作流对应模板正文（允许空模板），素材与模板同事务保存；兼容 format 1 旧包且保留当前模板。本地数据库升级保留原素材。
4. 已同步使用说明并完成自动回归。原有 15 组检查通过；tests/verify-update.cjs 新增检查通过，覆盖编辑状态、卡片点击、键盘、无模板、取消导入、模板刷新恢复、空模板、旧包兼容、无效模板拒绝、导出正文完整性、对比拖动与单击。两套检查均未捕获页面脚本错误。

测试使用独立浏览器会话，不修改实际预览中的用户案例。未新增默认模板或页面内模板编辑器。

## 工作流改名（2026-09-09）

编辑状态新增名称输入框与保存按钮，支持 Enter 保存，限制 1–40 字符。名称本地持久化，搜索同步更新，展示状态隐藏编辑入口。案例包升级 format 3，名称与素材、模板同事务导入，兼容旧包并保留现有名称。新增 tests/verify-names.cjs 验证保存、刷新、搜索、空名称拒绝、展示状态和模板关联。
