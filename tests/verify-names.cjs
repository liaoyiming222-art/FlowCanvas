const {chromium}=require('C:/Users/122305/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true});try{const page=await browser.newPage();await page.goto('http://127.0.0.1:4173');await page.waitForFunction(()=>!busy);const card=page.locator('[data-id="material"]');await card.locator('.workflow-name-editor input').fill('我的材质工作流');await card.getByText('保存名称',{exact:true}).click();await page.waitForFunction(()=>!busy);assert.equal(await card.locator('.case-title').innerText(),'我的材质工作流');await page.reload();await page.waitForFunction(()=>!busy);assert.equal(await card.locator('.case-title').innerText(),'我的材质工作流');await page.locator('#search').fill('我的材质');assert.equal(await page.locator('.case-card').count(),1);await card.locator('.workflow-name-editor input').fill('   ');await card.getByText('保存名称',{exact:true}).click();assert.equal(await card.locator('.case-title').innerText(),'我的材质工作流');await page.locator('#editCases').click();assert.equal(await page.locator('.workflow-name-editor').count(),0);await card.locator('.case-stage').click();assert.match(await page.locator('#prompt').inputValue(),/原始场景图/);console.log('PASS 名称保存、刷新恢复、搜索、空名称拒绝、展示隐藏、模板关联不变');}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
