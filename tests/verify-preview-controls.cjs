const {chromium}=require('C:/Users/122305/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true});try{const page=await browser.newPage({viewport:{width:1440,height:1000}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:4173');await page.waitForFunction(()=>!busy);
await page.evaluate(async()=>{const w=allWorkflows[0];const c=document.createElement('canvas');c.width=400;c.height=300;for(const [side,color] of [['before','#f99'],['after','#99f']]){c.getContext('2d').fillStyle=color;c.getContext('2d').fillRect(0,0,400,300);const b=await new Promise(r=>c.toBlob(r));await changeAsset(w,side,new File([b],side+'.png',{type:'image/png'}));}});
await page.locator('#editCases').click();const stage=page.locator('[data-id="material"] .case-stage');await stage.click({position:{x:20,y:20}});assert.equal(await stage.getAttribute('data-split'),'50');assert.ok((await page.locator('#prompt').inputValue()).length>0);
const handle=stage.locator('.compare-handle');await handle.focus();await page.keyboard.press('Home');assert.equal(await stage.locator('.before').isVisible(),false);await page.keyboard.press('End');assert.equal(await stage.locator('.after').isVisible(),false);await page.keyboard.press('ArrowLeft');assert.equal(await stage.getAttribute('data-split'),'98');
await page.locator('#prompt').fill('保留输入');await handle.scrollIntoViewIfNeeded();let h=await handle.boundingBox(),r=await stage.boundingBox();await page.mouse.move(h.x+22,h.y+22);await page.mouse.down();await page.mouse.move(r.x+r.width*.3,r.y+r.height/2,{steps:8});await page.mouse.up();assert.equal(await page.locator('#prompt').inputValue(),'保留输入');
await page.locator('[data-id="material"] .preview-button').click();assert.equal(await page.locator('#mediaDialog').isVisible(),true);await page.locator('#mediaDialog .compare-handle').focus();await page.keyboard.press('Home');assert.equal(await page.locator('#mediaDialog .before').isVisible(),false);assert.equal(await page.locator('#prompt').inputValue(),'保留输入');await page.screenshot({path:'tests/results/fullscreen-new.png'});await page.keyboard.press('Escape');
assert.equal(await page.locator('#parameterButton').innerText(),'智能 · 1K · 1');await page.locator('#parameterButton').click();await page.locator('[data-ratio="16:9"]').click();await page.locator('[data-group="resolution"]').getByText('2K',{exact:true}).click();await page.locator('[data-group="count"]').getByText('3',{exact:true}).click();assert.equal(await page.locator('#parameterButton').innerText(),'16:9 · 2K · 3');await page.screenshot({path:'tests/results/parameters-new.png'});
for(const width of [390,320]){await page.setViewportSize({width,height:844});await page.locator('#parameterButton').scrollIntoViewIfNeeded();const p=await page.locator('#autoPanel').boundingBox(),b=await page.locator('#parameterButton').boundingBox();assert.ok(p.x>=0&&p.x+p.width<=width);assert.ok(p.y>=b.y+b.height);assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),true);}await page.screenshot({path:'tests/results/parameters-mobile-new.png'});await page.keyboard.press('Escape');assert.equal(await page.locator('#autoPanel').isVisible(),false);assert.deepEqual(errors,[]);console.log('PASS 手柄拖动、整图点击不移动、端点标签、全屏对比、提示词隔离、参数摘要、移动端定位与关闭');}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
